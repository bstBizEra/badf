diff --git a/.github/workflows/badf-gates.yml b/.github/workflows/badf-gates.yml
index cc01141..369bc2b 100644
--- a/.github/workflows/badf-gates.yml
+++ b/.github/workflows/badf-gates.yml
@@ -52,6 +52,9 @@ jobs:
           python3 scripts/badf_gate.py research examples/research-record-shadow-composed-red.json
           python3 scripts/badf_gate.py research examples/research-record-shadow-ci-parity.json
           python3 scripts/badf_gate.py assure examples/architecture-assurance.json
+          python3 scripts/badf_gate.py assure examples/architecture-assurance-shadow-stdlib-compliant.json
+          python3 scripts/badf_gate.py assure examples/architecture-assurance-shadow-pyyaml-drift.json
+          python3 scripts/badf_gate.py assure examples/architecture-assurance-shadow-indeterminate.json
       - name: PR traceability (Work-Package + Closes #N)
         # BADF-MAIN-001. Only a PR has a body; a push to main has already been
         # traced by the PR that produced it. Reports on the PR event only.
diff --git a/badf/demands/BADF-DEM-0046.json b/badf/demands/BADF-DEM-0046.json
new file mode 100644
index 0000000..3926066
--- /dev/null
+++ b/badf/demands/BADF-DEM-0046.json
@@ -0,0 +1,22 @@
+{
+  "schema_version": "1.0.0",
+  "demand_id": "BADF-DEM-0046",
+  "kind": "issue",
+  "source": {
+    "repository": "bstBizEra/badf",
+    "issue": 108,
+    "url": "https://github.com/bstBizEra/badf/issues/108",
+    "exported_at": "2026-08-29T11:17:32Z",
+    "body_digest": "sha256:7eec04678f0b9a79aa973521a0b075a7e759fe456cc659f991e5ecb505bc5b9b"
+  },
+  "title": "GOV-0042: WP-ARCH-D \u2014 fix acceptance.md status drift + shadow-calibrate ASSURE; badf-architecture VALIDATED -> SHADOWED",
+  "problem": "## Observed\nTwo things about `badf-architecture` (currently `VALIDATED`):\n1. **Documentation-state drift (defect).** `references/acceptance.md` line 3 still says \"The capability is `DESIGNED`\" and line 48 still says \"Until then the registry entry stays `DESIGNED`\", while the same doc's middle correctly says \"VALIDATED: met (WP-ARCH-C)\" and the registry + `SKILL.md` say `VALIDATED`. Two conflicting normative statements in one skill \u2014 the \"claim that outlives its correction\" class.\n2. **No shadow calibration.** The ASSURE substrate (controls 13-18, WP-ARCH-C) is validated only against one synthetic example. The admission ladder puts `SHADOWED` before `APPROVED`/`ACTIVE`: run ASSURE retrospectively on real architecture-impacting cases, blind, and measure \u2014 before any advance toward ACTIVE.\n\n## Expected\nWP-ARCH-D:\n- Fix the acceptance.md status drift (line 3 and line 48 -> the `VALIDATED` reality).\n- Produce shadow calibration: **three gate-valid `architecture-assurance` records** over real BADF architecture cases spanning the outcome space (a `COMPLIANT` stdlib-boundary check; a `NONCOMPLIANT` dependency drift \u2014 the #57 PyYAML boundary break \u2014 with an `UNAUTHORIZED_DRIFT` classification and a MAJOR finding; an `INDETERMINATE` case where an ADR is not statically observable), plus a calibration note measuring true violations / false positives / INDETERMINATE handling / non-coverage / drift-not-self-approved.\n- Advance `badf-architecture` `VALIDATED -> SHADOWED` (the operator's stated next step; **not** ACTIVE).\n\n## Evidence\n- `acceptance.md:3` \"capability is DESIGNED\"; `acceptance.md:48` \"stays DESIGNED\" \u2014 contradict registry/SKILL.md `VALIDATED`\n- `schemas/architecture-assurance.schema.json` + the `assure` command exist (WP-ARCH-C)\n\n## Unknowns\nWhether the ASSURE contract faithfully represents each real case is the point of the calibration; a gap becomes its own issue. `APPROVED`/`ACTIVE` remain deferred (operator admission decision).\n\n## Proposed work package\n`BADF-WP-0059` (WP-ARCH-D). acceptance.md status fix; three shadow `architecture-assurance` records + CI; calibration note; advance `VALIDATED -> SHADOWED`; contract test; own G07 self-dossier. C2.\n\ndiscovered-by: operator review of badf-architecture (2026-08-29) \u00b7 status: DISCOVERED\n\n",
+  "authorized_by": {
+    "principal": "operator",
+    "principal_type": "human"
+  },
+  "recorded_at": "2026-08-29T11:17:32Z",
+  "provenance": "EXPORTED_FROM_SOURCE",
+  "status": "AUTHORIZED",
+  "note": "WP-ARCH-D: fix acceptance.md status drift + shadow-calibrate ASSURE on real cases + advance badf-architecture VALIDATED->SHADOWED (not ACTIVE). Authority: operator review 'fix the drift and execute WP-ARCH-D before ACTIVE' (2026-08-29). Worked as BADF-WP-0059."
+}
diff --git a/badf/skill-registry.json b/badf/skill-registry.json
index b3d1ed8..3a4bf87 100644
--- a/badf/skill-registry.json
+++ b/badf/skill-registry.json
@@ -58,8 +58,8 @@
       "version": "0.1.0",
       "risk_class": "C1",
       "allowed_tools": [],
-      "status": "VALIDATED",
-      "digest": "sha256:45bfb3345945549411448c9c13a588d02a75e7af7d2787fe5dada5d46eb3f9d6"
+      "status": "SHADOWED",
+      "digest": "sha256:5e262395a86899a4b01ef2b58847b50cbf9fa0ec48566c3863c689e03d9ad414"
     },
     {
       "name": "problem-framing",
diff --git a/docs/governance/GITHUB_CONTROL_PLANE.md b/docs/governance/GITHUB_CONTROL_PLANE.md
index a4c0bfe..35d37e0 100644
--- a/docs/governance/GITHUB_CONTROL_PLANE.md
+++ b/docs/governance/GITHUB_CONTROL_PLANE.md
@@ -510,6 +510,21 @@ authorities, remain between research and execution. This WP is governance-only (
 status; empty gate diff). `badf-architecture` sits at `VALIDATED`; `badf-research` is now the first
 research capability to reach `ACTIVE`.
 
+## ASSURE shadow calibration + a status-drift fix — VALIDATED → SHADOWED (`BADF-WP-0059`, Issue #108)
+
+WP-ARCH-D does two things the operator's review surfaced. First, it **fixes a documentation-state
+drift**: `badf-architecture`'s `acceptance.md` still called the capability `DESIGNED` (line 3) and said
+it "stays `DESIGNED`" (line 48) while the registry, `SKILL.md`, and the same doc's middle said
+`VALIDATED` — the claim-that-outlives-its-correction defect this repo keeps finding in itself; both
+stale lines are corrected. Second, it **shadow-calibrates the ASSURE substrate** on real BADF
+architecture cases spanning the outcome space: `COMPLIANT` (the stdlib-only boundary holds),
+`NONCOMPLIANT` (the #57 PyYAML dependency drift — a true `UNAUTHORIZED_DRIFT` / `NONCONFORMANT` with a
+MAJOR finding), and `INDETERMINATE` (an ADR not statically observable — which does *not* serialise as a
+pass, ARCH-I07/control 15). Measurement (`references/assurance-shadow-evidence.md`): 1/1 true violation
+detected, 0 false positives, INDETERMINATE handled without a false pass, no drift self-approved, every
+run declares non-coverage — **no contract gap surfaced**. `badf-architecture` advances
+`VALIDATED → SHADOWED`; `APPROVED`/`ACTIVE` remain the operator's admission decision.
+
 ## Discovery ≠ scope expansion
 
 Work on `BADF-WP-A` that finds problem B opens an Issue for B (`status: DISCOVERED`,
diff --git a/examples/architecture-assurance-shadow-indeterminate.json b/examples/architecture-assurance-shadow-indeterminate.json
new file mode 100644
index 0000000..7e64b1d
--- /dev/null
+++ b/examples/architecture-assurance-shadow-indeterminate.json
@@ -0,0 +1,72 @@
+{
+  "schema_version": "1.0.0",
+  "id": "BADF-ARA-0004",
+  "baseline": {
+    "repository": "bstBizEra/badf",
+    "revision": "2e46fe7",
+    "digest": "sha256:82b229e2ca3ed3426ae09ae21621fba75bb0af559d97f99d77f7c574dadb64c4"
+  },
+  "observed": {
+    "repository": "bstBizEra/badf",
+    "revision": "2e46fe7",
+    "observed_at": "2026-08-29T02:00:00Z"
+  },
+  "adr_set": [
+    "ADR-0001"
+  ],
+  "fitness_spec_set": [
+    "FIT-001"
+  ],
+  "adr_compliance": [
+    {
+      "adr": "ADR-0001",
+      "result": "INDETERMINATE"
+    }
+  ],
+  "drift": [
+    {
+      "id": "DR-001",
+      "category": "ADR_DRIFT",
+      "classification": "UNKNOWN",
+      "affected_elements": [
+        "EL-001"
+      ],
+      "statement": "whether a dynamically constructed import path stays stdlib-only cannot be determined by static analysis"
+    }
+  ],
+  "findings": [
+    {
+      "finding_id": "AF-001",
+      "kind": "ADR",
+      "severity": "MINOR",
+      "baseline_ref": "2e46fe7",
+      "observed_ref": "2e46fe7",
+      "affected_elements": [
+        "EL-001"
+      ],
+      "evidence_locations": [
+        "importlib usage (hypothetical)"
+      ],
+      "expected": "ADR-0001 stdlib-only is observable in the import graph",
+      "observed": "a dynamic import path defeats static resolution",
+      "impact": "compliance cannot be asserted or refuted statically",
+      "failure_scenario": "an ASSURE run that guessed COMPLIANT here would validate the implementation against itself",
+      "recommendation_direction": "require a runtime import audit, or forbid dynamic import construction by ADR",
+      "status": "OPEN",
+      "non_coverage": [
+        "runtime import behaviour"
+      ]
+    }
+  ],
+  "conclusion": "INDETERMINATE",
+  "non_coverage": [
+    {
+      "surface": "runtime import behaviour",
+      "reason": "static analysis cannot resolve dynamic import paths -- INDETERMINATE, not COMPLIANT"
+    }
+  ],
+  "authority": {
+    "implementation_authority": false
+  },
+  "recorded_at": "2026-08-29T02:14:00Z"
+}
diff --git a/examples/architecture-assurance-shadow-pyyaml-drift.json b/examples/architecture-assurance-shadow-pyyaml-drift.json
new file mode 100644
index 0000000..055dc66
--- /dev/null
+++ b/examples/architecture-assurance-shadow-pyyaml-drift.json
@@ -0,0 +1,70 @@
+{
+  "schema_version": "1.0.0",
+  "id": "BADF-ARA-0003",
+  "baseline": {
+    "repository": "bstBizEra/badf",
+    "revision": "59c5293",
+    "digest": "sha256:82b229e2ca3ed3426ae09ae21621fba75bb0af559d97f99d77f7c574dadb64c4"
+  },
+  "observed": {
+    "repository": "bstBizEra/badf",
+    "revision": "59c5293",
+    "observed_at": "2026-08-29T02:00:00Z"
+  },
+  "adr_set": [
+    "ADR-0001"
+  ],
+  "fitness_spec_set": [
+    "FIT-001"
+  ],
+  "adr_compliance": [
+    {
+      "adr": "ADR-0001",
+      "result": "NONCONFORMANT"
+    }
+  ],
+  "drift": [
+    {
+      "id": "DR-001",
+      "category": "DEPENDENCY_DRIFT",
+      "classification": "UNAUTHORIZED_DRIFT",
+      "affected_elements": [
+        "EL-001"
+      ],
+      "statement": "a test imported PyYAML, a non-stdlib dependency absent on the bare CI runner, breaking ADR-0001"
+    }
+  ],
+  "findings": [
+    {
+      "finding_id": "AF-001",
+      "kind": "DEPENDENCY",
+      "severity": "MAJOR",
+      "baseline_ref": "59c5293",
+      "observed_ref": "59c5293",
+      "affected_elements": [
+        "EL-001"
+      ],
+      "evidence_locations": [
+        "a test: import yaml"
+      ],
+      "expected": "the gate and its tests import only the standard library (ADR-0001)",
+      "observed": "import yaml in a test; ModuleNotFoundError on the bare runner",
+      "impact": "green local, red CI -- the faithful-runner property is violated",
+      "failure_scenario": "the composed-tree gate reports green locally while CI is red",
+      "recommendation_direction": "remove the non-stdlib import or guard it try/except ImportError",
+      "status": "OPEN",
+      "non_coverage": []
+    }
+  ],
+  "conclusion": "NONCOMPLIANT",
+  "non_coverage": [
+    {
+      "surface": "other optional-dependency imports",
+      "reason": "only the yaml import was in scope"
+    }
+  ],
+  "authority": {
+    "implementation_authority": false
+  },
+  "recorded_at": "2026-08-29T02:12:00Z"
+}
diff --git a/examples/architecture-assurance-shadow-stdlib-compliant.json b/examples/architecture-assurance-shadow-stdlib-compliant.json
new file mode 100644
index 0000000..5d388d8
--- /dev/null
+++ b/examples/architecture-assurance-shadow-stdlib-compliant.json
@@ -0,0 +1,60 @@
+{
+  "schema_version": "1.0.0",
+  "id": "BADF-ARA-0002",
+  "baseline": {
+    "repository": "bstBizEra/badf",
+    "revision": "bc9eb30",
+    "digest": "sha256:82b229e2ca3ed3426ae09ae21621fba75bb0af559d97f99d77f7c574dadb64c4"
+  },
+  "observed": {
+    "repository": "bstBizEra/badf",
+    "revision": "bc9eb30",
+    "observed_at": "2026-08-29T02:00:00Z"
+  },
+  "adr_set": [
+    "ADR-0001"
+  ],
+  "fitness_spec_set": [
+    "FIT-001"
+  ],
+  "adr_compliance": [
+    {
+      "adr": "ADR-0001",
+      "result": "CONFORMANT"
+    }
+  ],
+  "drift": [],
+  "findings": [
+    {
+      "finding_id": "AF-001",
+      "kind": "DEPENDENCY",
+      "severity": "INFO",
+      "baseline_ref": "bc9eb30",
+      "observed_ref": "bc9eb30",
+      "affected_elements": [
+        "EL-001"
+      ],
+      "evidence_locations": [
+        "scripts/badf_gate.py imports"
+      ],
+      "expected": "the gate imports only the standard library (ADR-0001)",
+      "observed": "only stdlib imports present",
+      "impact": "none; the stdlib-only boundary holds",
+      "failure_scenario": "a third-party import would break the faithful-runner property",
+      "recommendation_direction": "keep stdlib-only",
+      "status": "ACKNOWLEDGED",
+      "non_coverage": []
+    }
+  ],
+  "conclusion": "COMPLIANT",
+  "non_coverage": [
+    {
+      "surface": "dynamically imported modules",
+      "reason": "not statically resolvable"
+    }
+  ],
+  "authority": {
+    "implementation_authority": false
+  },
+  "recorded_at": "2026-08-29T02:10:00Z"
+}
diff --git a/skills/badf-architecture/SKILL.md b/skills/badf-architecture/SKILL.md
index 00d7b42..6ccc24c 100644
--- a/skills/badf-architecture/SKILL.md
+++ b/skills/badf-architecture/SKILL.md
@@ -5,7 +5,7 @@ description: Govern architecture design, architectural decisions, architecture f
 
 # BADF Architecture
 
-Status: `VALIDATED` (contract frozen by `BADF-WP-0043`; the five G04 DESIGN evidence types enforced by `BADF-WP-0044`; the ASSURE substrate -- controls 13-18 -- enforced by `BADF-WP-0057` / WP-ARCH-C via the `assure` gate command). `SHADOWED` / `APPROVED` / `ACTIVE` remain the operator's admission decisions. This is an architecture **engineering + assurance** capability, not a diagram skill. Its primary lifecycle gate is **G04 — Architecture, Data and API**; it also feeds G05 (trust boundaries + data flows), G08/G09 verification and post-merge drift review.
+Status: `SHADOWED` (contract frozen by `BADF-WP-0043`; the five G04 DESIGN evidence types enforced by `BADF-WP-0044`; the ASSURE substrate -- controls 13-18 -- enforced by `BADF-WP-0057` / WP-ARCH-C; shadow-calibrated on real architecture cases by `BADF-WP-0059` / WP-ARCH-D). `APPROVED` / `ACTIVE` remain the operator's admission decisions. This is an architecture **engineering + assurance** capability, not a diagram skill. Its primary lifecycle gate is **G04 — Architecture, Data and API**; it also feeds G05 (trust boundaries + data flows), G08/G09 verification and post-merge drift review.
 
 Fundamental rule — four distinctions that never collapse:
 
diff --git a/skills/badf-architecture/references/acceptance.md b/skills/badf-architecture/references/acceptance.md
index 23b1c2b..7ce85ca 100644
--- a/skills/badf-architecture/references/acceptance.md
+++ b/skills/badf-architecture/references/acceptance.md
@@ -1,6 +1,6 @@
 # Acceptance, invariants and admission
 
-Status: frozen contract v0.1. The capability is `DESIGNED`. Progression follows `docs/07-skills-governance.md`: `DESIGNED → IMPLEMENTED → VALIDATED → SHADOWED → APPROVED → ACTIVE`.
+Status: frozen contract v0.1. The capability is `SHADOWED` (advanced `DESIGNED -> IMPLEMENTED -> VALIDATED -> SHADOWED` across WP-ARCH-A/B/C/D). Progression follows `docs/07-skills-governance.md`: `DESIGNED → IMPLEMENTED → VALIDATED → SHADOWED → APPROVED → ACTIVE`.
 
 ## Canonical invariants
 
@@ -44,5 +44,5 @@ Status: frozen contract v0.1. The capability is `DESIGNED`. Progression follows
 
 - `IMPLEMENTED`: the G04 DESIGN evidence semantics (`architecture`, `adr`, `data-model`, `api-contract`, `operability-design`) exist as deterministic gate rules with schemas (WP-ARCH-B).
 - `VALIDATED`: **met (`BADF-WP-0057` / WP-ARCH-C).** The ASSURE substrate is deterministic -- controls 13-18 enforced by the `assure` gate command with mutation-tested tests; the `architecture-assurance` record binds one baseline + one observed revision, never infers compliance, never self-authorises drift, and grants no authority. The registry status is advanced to `VALIDATED`.
-- `SHADOWED`: ASSURE is run retrospectively on historical architecture-impacting PRs without knowing the answer (WP-ARCH-D); measured for true violations, false positives, non-coverage and stability across composed trees.
-- `APPROVED` / `ACTIVE`: owner and security approval; registry digest pinned. Until then the registry entry stays `DESIGNED`.
+- `SHADOWED`: **met (`BADF-WP-0059` / WP-ARCH-D).** ASSURE run retrospectively on real BADF architecture cases spanning the outcome space (COMPLIANT stdlib-boundary; NONCOMPLIANT dependency drift; INDETERMINATE unobservable ADR); measured for true violations, false positives, INDETERMINATE handling and declared non-coverage -- no contract gap surfaced. See `references/assurance-shadow-evidence.md`. Registry status advanced to `SHADOWED`.
+- `APPROVED` / `ACTIVE`: owner and security approval; registry digest pinned. These remain the operator's admission decision; `badf-architecture` sits at `SHADOWED` until then.
diff --git a/skills/badf-architecture/references/assurance-shadow-evidence.md b/skills/badf-architecture/references/assurance-shadow-evidence.md
new file mode 100644
index 0000000..85d64ea
--- /dev/null
+++ b/skills/badf-architecture/references/assurance-shadow-evidence.md
@@ -0,0 +1,29 @@
+# ASSURE shadow calibration (`BADF-WP-0059`, Issue #108)
+
+Before `APPROVED`/`ACTIVE`, the ASSURE substrate is run retrospectively on **real BADF architecture
+cases**, spanning the outcome space, and measured — would it detect the real violation, refuse to
+manufacture compliance, and handle the undecidable case without a false pass?
+
+Three gate-valid `architecture-assurance` records:
+
+| Record | Conclusion | Real case | What the contract had to do | Result |
+| :--- | :--- | :--- | :--- | :--- |
+| `…-stdlib-compliant.json` | `COMPLIANT` | the stdlib-only boundary (ADR-0001) holds at `bc9eb30` | bind a baseline + observed revision, ADR `CONFORMANT`, no drift | Faithful — COMPLIANT rests on a bound baseline digest (control 14). |
+| `…-pyyaml-drift.json` | `NONCOMPLIANT` | the #57 PyYAML import broke the stdlib boundary at `59c5293` | detect a real `DEPENDENCY_DRIFT`, classify `UNAUTHORIZED_DRIFT` (not approved), ADR `NONCONFORMANT`, MAJOR finding | **True violation detected.** The drift is not self-approved (control 16); the finding carries expected/observed/failure-scenario/evidence. |
+| `…-indeterminate.json` | `INDETERMINATE` | ADR-0001 compliance is not statically observable for a dynamic import path at `2e46fe7` | refuse to guess COMPLIANT; conclusion `INDETERMINATE`, ADR `INDETERMINATE`, non-coverage declared | **No false pass.** `INDETERMINATE` did not serialise as a pass (control 15); `NO BASELINE ≠ COMPLIANT` / *don't validate the implementation against itself* held (ARCH-I07). |
+
+## Measurement
+
+| Metric | Result |
+| :--- | :--- |
+| true violations detected | 1/1 (the PyYAML drift) |
+| false positives | 0 (the COMPLIANT case is genuinely conformant) |
+| INDETERMINATE handled without a false pass | yes (control 15) |
+| drift self-approved | none (control 16 forbids it) |
+| non-coverage declared | every run (control 17) |
+| single-baseline discipline | every finding bound to the one baseline (control 18) |
+| read-only | `implementation_authority` fixed `false` in every record (ARCH-I12) |
+
+**No contract gap surfaced under real conditions.** This is the `SHADOWED` evidence; `badf-architecture`
+advances `VALIDATED → SHADOWED`. `APPROVED`/`ACTIVE` remain the operator's admission decision, now
+backed by calibration data rather than one synthetic example.
diff --git a/tests/test_badf_architecture_contract.py b/tests/test_badf_architecture_contract.py
index 9828a8c..bde56c0 100644
--- a/tests/test_badf_architecture_contract.py
+++ b/tests/test_badf_architecture_contract.py
@@ -72,13 +72,19 @@ class InvariantTests(unittest.TestCase):
 
 class RegistryAndAuthorityTests(unittest.TestCase):
 
-    def test_registry_entry_is_validated_with_a_real_digest(self):
-        # WP-ARCH-C advanced the capability IMPLEMENTED -> VALIDATED (the ASSURE substrate is deterministic; controls 13-18 pass with mutation).
+    def test_registry_entry_is_shadowed_with_a_real_digest(self):
+        # WP-ARCH-D advanced the capability VALIDATED -> SHADOWED (ASSURE calibrated on real cases).
         reg = json.loads((gate.ROOT / "badf/skill-registry.json").read_text())
         entry = next(e for e in reg["skills"] if e["name"] == "badf-architecture")
-        self.assertEqual(entry["status"], "VALIDATED")
+        self.assertEqual(entry["status"], "SHADOWED")
         self.assertEqual(entry["digest"], "sha256:" + hashlib.sha256((gate.ROOT / entry["source"]).read_bytes()).hexdigest())
 
+    def test_acceptance_doc_has_no_stale_designed_status(self):
+        # WP-ARCH-D fixed the doc drift: acceptance.md must not still call the capability DESIGNED.
+        acc = (REF / "acceptance.md").read_text(encoding="utf-8")
+        self.assertNotIn("The capability is `DESIGNED`", acc)
+        self.assertNotIn("stays `DESIGNED`", acc)
+
     def test_g04_is_mapped_but_unchanged(self):
         g04text = (REF / "g04-contract.md").read_text(encoding="utf-8")
         for t in G04_EVIDENCE:
diff --git a/tests/test_badf_assure.py b/tests/test_badf_assure.py
index 993bba1..bc73e51 100644
--- a/tests/test_badf_assure.py
+++ b/tests/test_badf_assure.py
@@ -73,5 +73,30 @@ class AssureTests(unittest.TestCase):
         self.assertEqual(r.returncode, 0, r.stderr); self.assertIn("ASSURE PASS", r.stdout)
 
 
+class ShadowCalibrationTests(unittest.TestCase):
+    """BADF-WP-0059 (WP-ARCH-D): the three shadow assurance records over real BADF
+    architecture cases are gate-valid and span the outcome space."""
+
+    SHADOWS = {"stdlib-compliant": "COMPLIANT", "pyyaml-drift": "NONCOMPLIANT", "indeterminate": "INDETERMINATE"}
+
+    def test_all_three_shadow_records_pass_and_span_the_outcomes(self):
+        seen = set()
+        for name, expected in self.SHADOWS.items():
+            rec = json.loads((gate.ROOT / f"examples/architecture-assurance-shadow-{name}.json").read_text())
+            self.assertEqual(rec["conclusion"], expected)
+            r = subprocess.run([sys.executable, "scripts/badf_gate.py", "assure",
+                                str(gate.ROOT / f"examples/architecture-assurance-shadow-{name}.json")],
+                               cwd=str(gate.ROOT), capture_output=True, text=True)
+            self.assertEqual(r.returncode, 0, f"{name}: {r.stderr}")
+            seen.add(rec["conclusion"])
+        self.assertEqual(seen, {"COMPLIANT", "NONCOMPLIANT", "INDETERMINATE"})
+
+    def test_the_drift_case_is_a_true_unauthorised_violation(self):
+        rec = json.loads((gate.ROOT / "examples/architecture-assurance-shadow-pyyaml-drift.json").read_text())
+        self.assertEqual(rec["drift"][0]["classification"], "UNAUTHORIZED_DRIFT")
+        self.assertEqual(rec["adr_compliance"][0]["result"], "NONCONFORMANT")
+        self.assertTrue(any(f["severity"] == "MAJOR" for f in rec["findings"]))
+
+
 if __name__ == "__main__":
     unittest.main()
diff --git a/work/WP-2026-0058/work-package.json b/work/WP-2026-0058/work-package.json
index a196b17..8c21254 100644
--- a/work/WP-2026-0058/work-package.json
+++ b/work/WP-2026-0058/work-package.json
@@ -43,11 +43,12 @@
     "reversible": true,
     "method": "revert the squash commit via PR"
   },
-  "status": "IN_PROGRESS",
+  "status": "CLOSED",
   "external_target": {
     "repository": "bstBizEra/badf",
     "branch": "main",
-    "base_revision": "2e46fe7"
+    "base_revision": "2e46fe7",
+    "landed_as": "73a2b173ea075a99e72d0b434947c2dc47bfb908"
   },
   "note": "Lands IN_PROGRESS by design; the next WP reconciles it. Governance-only WP (docs + registry status) -- no gate control (empty gate diff). Advances badf-research to ACTIVE on the operator's explicit 'finish badf-research' approval. Reconciles WP-2026-0057. Grants no authority even ACTIVE (RSR-I01)."
 }
