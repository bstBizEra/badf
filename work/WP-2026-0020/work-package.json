{
  "$schema": "../../schemas/work-package.schema.json",
  "schema_version": "1.0.0",
  "id": "WP-2026-0020",
  "title": "Hot-fix: reconciliation fixture normalises inherited ledger debt; post-merge simulation before merge",
  "owner": "human_sponsor",
  "repository": "bstBizEra/badf",
  "demand": "BADF-DEM-0007",
  "objective": "Make main green again at the tree that lands, and make the reconciliation tests true on main after every future merge: the fixture corroborates every record the ledger shows landed (independent parse) and each test adds only the debt it asserts on.",
  "business_value": "main has been red since 30186c5. BRANCH_GREEN != MERGE_SAFE was demonstrated by the WP that introduced the ledger rule; the fix must be verified against the post-merge composed tree, not the PR runner's pre-merge origin/main.",
  "in_scope": [
    "tests/test_badf_reconcile.py fixture: ledger() + normalisation",
    "reconcile WP-2026-0019 (rule d, first step of this branch)",
    "GITHUB_CONTROL_PLANE.md: post-merge simulation step",
    "post-merge simulation performed and cited before merge"
  ],
  "out_of_scope": [
    "a composed-tree merge gate in CI (#21)",
    "any gate logic change"
  ],
  "target_gate": "G07",
  "change_class": "C2",
  "data_classification": "internal",
  "acceptance_criteria": [
    "reconcile module green with origin/main = 30186c5 (WP-0019 debt present)",
    "reconcile module green in a simulated post-merge tree where this WP is the debt",
    "full suite green on the committed tree and in CI shape",
    "push-to-main run for the squash is green"
  ],
  "permissions": [
    "write: bstBizEra/badf via PR"
  ],
  "tests": [
    "tests/test_badf_reconcile.py"
  ],
  "evidence": [
    "post-merge simulation output cited in the PR",
    "push-to-main run conclusion"
  ],
  "rollback": {
    "reversible": true,
    "method": "revert the squash commit via PR"
  },
  "status": "IN_PROGRESS",
  "external_target": {
    "repository": "bstBizEra/badf",
    "branch": "main",
    "base_revision": "30186c5"
  },
  "note": "Lands IN_PROGRESS by design; LANDED_UNRECONCILED on main until the next WP reconciles it."
}
