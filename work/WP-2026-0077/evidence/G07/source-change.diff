diff --git a/badf/demands/BADF-DEM-0064.json b/badf/demands/BADF-DEM-0064.json
new file mode 100644
index 0000000..fdfb101
--- /dev/null
+++ b/badf/demands/BADF-DEM-0064.json
@@ -0,0 +1,22 @@
+{
+  "schema_version": "1.0.0",
+  "demand_id": "BADF-DEM-0064",
+  "kind": "issue",
+  "source": {
+    "repository": "bstBizEra/badf",
+    "issue": 146,
+    "url": "https://github.com/bstBizEra/badf/issues/146",
+    "exported_at": "2026-08-29T20:21:40Z",
+    "body_digest": "sha256:f660c10d1f0649db60a0fc0be60d93824fa4776fb57f2c09c3b76125ee195660"
+  },
+  "title": "GOV-0056: WP-SOL-E \u2014 admit badf-solution-design SHADOWED \u2192 ACTIVE (representative shadow accepted; re-shadow deferred #145)",
+  "problem": "## Observed\n\n`badf-solution-design` is `SHADOWED` (WP-SOL-D / #143) on a REPRESENTATIVE shadow \u2014 BADF has no real project compositions yet. The next and final admission rung is `ACTIVE`, which is a human admission gate (self-approval is prohibited).\n\n## Expected\n\n**WP-SOL-E \u2014 admit `SHADOWED \u2192 ACTIVE`.** The operator has made the admission decision to activate now, accepting the representative shadow, with the real-project re-shadow tracked as a deferred follow-up (#145). Registry `badf-solution-design` status `SHADOWED \u2192 ACTIVE`; the representative caveat is preserved (shadow-evidence.md still points at #145); ACTIVE grants NO new authority (SOL-I12, the one canonical gate stays sole authority); no lifecycle change, no new control, no second validator. Mirrors badf-research WP-0058 and badf-architecture WP-0065.\n\n## Evidence\n\n- Operator admission decision recorded 2026-08-30 (AskUserQuestion: 'Activate now, accept representative').\n- shadow-evidence.md (WP-SOL-D) is the calibration backing the admission; #145 tracks the owed real re-shadow.\n\n## Unknowns\n\nNone that block admission \u2014 the representative-vs-real risk is explicitly accepted by the operator and bounded by the deferred re-shadow (#145).\n\ndiscovered-by: BADF-WP-0074 (badf-solution-design landed SHADOWED) \u00b7 status: DISCOVERED\n",
+  "authorized_by": {
+    "principal": "operator",
+    "principal_type": "human"
+  },
+  "recorded_at": "2026-08-29T20:21:40Z",
+  "provenance": "EXPORTED_FROM_SOURCE",
+  "status": "AUTHORIZED",
+  "note": "WP-SOL-E admits badf-solution-design SHADOWED->ACTIVE on the operator's explicit admission decision (activate now, accept representative shadow; AskUserQuestion 2026-08-30). Grants no new authority (SOL-I12). Preserves the representative caveat; real re-shadow deferred to #145. DEM-0064/GOV-0056/WP-0077. Reconciles WP-2026-0074 (landed dc6b608)."
+}
diff --git a/badf/skill-registry.json b/badf/skill-registry.json
index 6c65907..08655fd 100644
--- a/badf/skill-registry.json
+++ b/badf/skill-registry.json
@@ -208,7 +208,7 @@
       "version": "0.1.0",
       "risk_class": "C1",
       "allowed_tools": [],
-      "status": "SHADOWED",
+      "status": "ACTIVE",
       "digest": "sha256:5d8848ef97cdd21c62cccfa4ff37b30f71823516566638fbee3cf1d60530ea46"
     },
     {
diff --git a/docs/governance/GITHUB_CONTROL_PLANE.md b/docs/governance/GITHUB_CONTROL_PLANE.md
index 695a263..2610b45 100644
--- a/docs/governance/GITHUB_CONTROL_PLANE.md
+++ b/docs/governance/GITHUB_CONTROL_PLANE.md
@@ -842,6 +842,23 @@ coverage; the gaps are named, not implied to pass. Two committed shadow matrices
 `lifecycle.json` change, no new control (the controls are WP-SOL-B/C), no second validator. Registry
 `VALIDATED → SHADOWED`; `APPROVED`/`ACTIVE` remains the operator's admission decision.
 
+## badf-solution-design → ACTIVE — operator admission on a representative shadow (`BADF-WP-0077` / WP-SOL-E, Issue #146 / GOV-0056)
+
+`SHADOWED → ACTIVE` is a **human admission gate** — self-approval is prohibited, so no agent crosses
+it. The operator made the admission decision (2026-08-30): **activate now, accepting the
+representative shadow**, rather than hold `ACTIVE` indefinitely for a real-project composition that
+BADF — a governance framework with no product surface — may not produce soon. This WP records that
+decision as a governed registry advance, mirroring `badf-research` (WP-0058) and `badf-architecture`
+(WP-0065): `ACTIVE` **grants the skill no new authority** (SOL-I12 — the one canonical
+`badf_gate.py solution` stays the sole composition authority; `badf_gate.py` and the platform rules
+remain the authorities), and there is no `lifecycle.json` change, no new control, no second validator.
+
+The admission does **not erase the caveat**: `references/shadow-evidence.md` still labels the shadow
+representative, and the owed real-project re-shadow is filed as a tracked follow-up
+(**#145**, `DISCOVERED`, trigger = the first real project composition). So the ladder stays truthful —
+the capability is admitted for use, its trust is honestly bounded to a representative shadow, and the
+debt to re-shadow on real data is tracked rather than lost. Registry `SHADOWED → ACTIVE`.
+
 ## Discovery ≠ scope expansion
 
 Work on `BADF-WP-A` that finds problem B opens an Issue for B (`status: DISCOVERED`,
diff --git a/skills/badf-solution-design/references/acceptance.md b/skills/badf-solution-design/references/acceptance.md
index 8d80a15..705f17f 100644
--- a/skills/badf-solution-design/references/acceptance.md
+++ b/skills/badf-solution-design/references/acceptance.md
@@ -14,8 +14,8 @@ contract first, prove it later, in separate WPs. **Do not build every specialist
 | **WP-SOL-A** (this) | root `SKILL.md` + references: composition model, routing, specialist ownership boundaries, cross-artifact traceability, SOL-I01…I12, G03/G04 mapping, architecture interaction, authority boundaries, external-methodology dispositions | `DESIGNED` |
 | WP-SOL-B | composition schemas + the solution-composition matrix artifact + deterministic controls | `IMPLEMENTED` |
 | WP-SOL-C | cross-artifact seam controls in the canonical `badf_gate.py` (failing-first) | `VALIDATED` |
-| WP-SOL-D | historical shadow calibration on real BADF solutions | `SHADOWED` |
-| — | owner / assurance admission, then registry activation | `APPROVED` → `ACTIVE` |
+| WP-SOL-D | **representative** shadow calibration (no real project compositions exist yet; real re-shadow deferred to #145) | `SHADOWED` |
+| WP-SOL-E | operator admission on the representative shadow (caveat preserved; #145 tracks the owed real re-shadow) → registry activation | `ACTIVE` |
 
 ## WP-SOL-A boundaries (no scope creep)
 
diff --git a/tests/test_badf_solution_composition.py b/tests/test_badf_solution_composition.py
index 0dda616..13263d8 100644
--- a/tests/test_badf_solution_composition.py
+++ b/tests/test_badf_solution_composition.py
@@ -135,10 +135,10 @@ class ShadowCalibrationTests(SolutionCompositionBase):
 
 
 class RegistryStatusTests(unittest.TestCase):
-    def test_badf_solution_design_is_registered_shadowed(self):  # WP-SOL-D advanced VALIDATED -> SHADOWED
+    def test_badf_solution_design_is_registered_active(self):  # WP-SOL-E admitted SHADOWED -> ACTIVE (operator admission)
         reg = json.loads((gate.ROOT / "badf/skill-registry.json").read_text())
         entry = next(e for e in reg["skills"] if e["name"] == "badf-solution-design")
-        self.assertEqual(entry["status"], "SHADOWED")
+        self.assertEqual(entry["status"], "ACTIVE")
 
 
 if __name__ == "__main__":
diff --git a/work/WP-2026-0074/work-package.json b/work/WP-2026-0074/work-package.json
index 85a956c..f045f8a 100644
--- a/work/WP-2026-0074/work-package.json
+++ b/work/WP-2026-0074/work-package.json
@@ -44,11 +44,12 @@
     "reversible": true,
     "method": "revert the squash commit via PR"
   },
-  "status": "IN_PROGRESS",
+  "status": "CLOSED",
   "external_target": {
     "repository": "bstBizEra/badf",
     "branch": "main",
-    "base_revision": "f1a0a5c"
+    "base_revision": "f1a0a5c",
+    "landed_as": "dc6b608ebea7aee2097b050883f0a0fe9ce9ed6c"
   },
   "note": "Lands IN_PROGRESS by design; the next WP reconciles it. Shadow evidence + status advance; no new gate control (the controls are WP-SOL-B/C), so no mutation. Built in an isolated worktree (badf-wt-sold, GIT-I04) parallel to BARCHI-2's badf-git GIT-D (#140). Ids WP-0074/DEM-0061/GOV-0053 (0073 poisoned by third actor, avoided). Reconciles WP-2026-0075 (GIT-D, landed f1a0a5c; WP-0069 already reconciled on main by GIT-D). Shipped via operator /loop 'badf-solution-design to active'. Resolves #141 / GOV-0053."
 }
